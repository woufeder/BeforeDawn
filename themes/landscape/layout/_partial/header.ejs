<header id="header" class="container flex flex-row py-4">

  <div id="logo" class="w-full">
    <a href="<%= url_for('/' + page.lang + '/') %>" class="inline-block w-fit">
      <h2 class="w-fit">夜明曉更前</h2>
      <h4 class="subLogo w-fit">Anyway Before Dawn</h4>
    </a>
  </div>
  <div class="ms-auto w-full flex items-end justify-end">
    <nav id="main-nav">
      <a id="main-nav-toggle" class="nav-icon"><span class="fa fa-bars"></span></a>
      <% for (var i in theme.menu){ if (i === 'home') continue;%>
        <% if (i==='original' ) { %>
          <div class="main-nav-item has-submenu">
            <a class="main-nav-link" href="<%- bd_url_for_lang(theme.menu[i]) %>">
              <%= bd_i18n(i) %>
            </a>
            <div class="main-nav-submenu">
              <a class="main-nav-submenu-link" href="<%- bd_url_for_lang('/blog/') %>">
                <%= bd_i18n("blog") %>
              </a>
            </div>
          </div>
          <% } else { %>
            <a class="main-nav-link" href="<%- bd_url_for_lang(theme.menu[i]) %>">
              <%= bd_i18n(i) %>
            </a>
            <% } %>
              <% } %>
    </nav>
  </div>
</header>