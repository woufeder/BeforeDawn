---
layout: page/layout
---

<section id="SAR" style="--box-bg-color: <%= page.blockBgColor %>;" class="fade-in-once">
  <section id="banner" class="box">
    <div class="left">
      <h1>雨後朝陽</h1>
      <h2>Sunrise After Rain</h2>
    </div>
    <div class="right flex flex-col items-center">
      <div class="hover-box flex flex-col items-center gap-8 text-center w-fit">
        <%- partial('_partial/svg/branch_top') %>
        <div class="grid">
          <div class="context default flex items-center justify-center">
            <h6 class="">
              剛下過雨的清晨，朝光自雲隙漏滲。地上佈滿水窪，行走時需要小心翼翼地避開。<br>
              夜雨洗卻入夏時的暑氣，潮濕空氣沁潤肺腑，心靈上的躁動也一併安沉。<br>
              一切看起來是那麼透明。不管是怎麼樣的難題，從此開始都能得到解答。<br><br>
              它是這樣的故事，所以就決定了這個名字。
            </h6>
          </div>
          <div class="context hover flex flex-col items-center justify-center">
            <h6 class="text-center">
              故事背景位於現代台灣的架空北部都市，以日常校園為主。部分情節與現實稍有不符。<br>
              時間軸為「未竟之聲」的七年前。並非前傳性質，可獨立閱讀不影響理解。
            </h6>
          </div>
        </div>
        <%- partial('_partial/svg/branch_bottom') %>
      </div>
    </div>
  </section>

  <section id="characters-area" class=" mt-10">
    <div class="p-8 box w-fit">
      <div class="flex flex-row items-center mb-6">
        <h2>
          MAIN CHARACTERS
        </h2>
        <!-- <a href="https://docs.google.com/spreadsheets/d/1Cxz4chKcb1_rkF7xz_W2xEI7c2XZCl3L5kT-26hfEJY/edit?gid=905747692#gid=905747692" class="btn ms-auto">詳細設定</a> -->
      </div>

      <div class="name-list columns-2 h-full text-left gap-8">

        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>久世　雪葉</p>
            <p>クセ　ユキバ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>風見　晴音</p>
            <p>カザミ　ハルネ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>時雨　日乃里</p>
            <p>シグレ　ヒノリ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>榊　秋光</p>
            <p>サカキ　アキミツ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>葛城　翔</p>
            <p>カツラギ　カケル</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>久世　夏花</p>
            <p>クセ　ナツカ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>辻原　浅名</p>
            <p>ツジハラ　アサナ</p>
          </div>
        </div>
        <div class="character-card">
          <div class="avatar">
          </div>
          <div>
            <p>大空　海</p>
            <p>オオゾラ　カイ</p>
          </div>
        </div>
      </div>
    </div>
    <div class="p-8 box w-full">
      <div class="flex flex-row items-center mb-6 ">
        <h2>
          TERMINOLOGY
        </h2>
      </div>
      <div class="noun-list">
        <table>
          <tbody>
            <tr class="border-dashed border border-gray-500 my-1">
              <td class="text-right mr-1 p-2  border-dashed border-r border-gray-500">岩城縣<br>九重市</td>
              <td class="p-2">位於日本的架空都市，鄰近飛驒山脈，地勢較高故平均氣溫低。人口年齡層偏高，多數遷居者都是為了方便就學。剩下的便是從祖上開始就定居的世家。</td>
            </tr>
            <tr class="border-dashed border border-gray-500 my-1">
              <td class="text-right mr-1 p-2 border-dashed border-r border-gray-500">神門大學附屬高等學校</td>
              <td class="p-2">隸屬學校法人神門大學的中高大一貫制高中，通稱「神大附」或「神門高」。僅招收具靈使資質，或有其餘特殊能力的學生。</td>
            </tr>
            <tr class="border-dashed border border-gray-500 my-1">
              <td class="text-right mr-1 p-2 border-dashed border-r border-gray-500">靈使</td>
              <td class="p-2">擁有將自身靈魂具象化的能力者。靈魂具象化的型態為實際存在的物體，通常為非生命，但也有部分人的靈具象化為生物，但必定不是人類。</td>
            </tr>
            <tr class="border-dashed border border-gray-500 my-1">
              <td class="text-right mr-1 p-2 border-dashed border-r border-gray-500">組別</td>
              <td class="p-2">神門高每個年級僅有三班，1組為神學組，2組為一般組，3組為特招組。</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>
  <section id="creates-area" class=" mt-10">
    <div class="p-8 box">
      <h2 class="mb-4">
        ILLUSTRATION
      </h2>
      <div class="flex flex-row justify-center">
        <div id="gallery">
          <div class="gallery-sizer"></div>
        </div>
      </div>
    </div>
    <div class="p-8 box">
      <h2 class="mb-4">
        STORY
      </h2>
      <% 
        const currentLang=bd_current_lang(); 
        let introPosts=[]; site.posts.each(function(post){ 
          if (bd_post_lang(post)!==currentLang) return; 
          var matched=false; post.categories.each(function(cat){ if(String(cat.name || '' ).trim()==='自介' ) { matched=true; } }); 
          if (matched) { introPosts.push(post); } }); 
          introPosts.sort(function(a, b){ return b.date.valueOf() - a.date.valueOf(); }); 
        %>
      <div id="about-intro-posts" class="px-6">
        <% introPosts.forEach(function(post, i){ %>
        <div class="intro-item" data-index="<%= i %>">
          <%- partial('_partial/article-list', {post: post, index: true}) %>
        </div>
        <% }) %>
      </div>
      <div id="intro-pagination" class="flex flex-row items-center justify-center gap-4">
        <button id="prev">上一頁</button>
        <span id="page-info"></span>
        <button id="next">下一頁</button>
      </div>
    </div>
  </section>
</section>

<script>
  (() => {
    const perPage = 5;
    const items = document.querySelectorAll('.intro-item');

    let currentPage = 1;
    const totalPages = Math.ceil(items.length / perPage);

    function render() {
      items.forEach((el, i) => {
        el.style.display =
          (i >= (currentPage - 1) * perPage && i < currentPage * perPage) ?
          'block' :
          'none';
      });

      document.getElementById('page-info').textContent =
        `${currentPage} / ${totalPages}`;
    }

    document.getElementById('prev').onclick = () => {
      if (currentPage > 1) {
        currentPage--;
        render();
      }
    };

    document.getElementById('next').onclick = () => {
      if (currentPage < totalPages) {
        currentPage++;
        render();
      }
    };

    render();
  })();
</script>
<script>
  (function() {
    const cloudName = 'dhtsewjf1';
    const tag = 'IB';

    async function loadGallery() {
      const gallery = document.getElementById('gallery');
      if (!gallery) return;

      // 清空舊的（保留 sizer）
      gallery.innerHTML = '<div class="gallery-sizer"></div>';

      try {
        const res = await fetch(
          `https://res.cloudinary.com/${cloudName}/image/list/${tag}.json`
        );
        const data = await res.json();

        const base = `https://res.cloudinary.com/${cloudName}/image/upload/`;

        data.resources.forEach(item => {
          const full = base + item.public_id + '.jpg';
          const thumb = base + 'q_auto,f_auto,w_400/' + item.public_id + '.jpg';

          const itemDiv = document.createElement('div');
          itemDiv.className = 'gallery-item';

          const a = document.createElement('a');
          a.href = full;
          a.setAttribute('data-fancybox', 'gallery');

          const img = document.createElement('img');
          img.src = thumb;

          a.appendChild(img);
          itemDiv.appendChild(a);
          gallery.appendChild(itemDiv);
        });

        imagesLoaded(gallery, function() {
          new Masonry(gallery, {
            itemSelector: '.gallery-item',
            columnWidth: '.gallery-sizer'
          });
        });

        if (window.Fancybox) {
          Fancybox.bind('[data-fancybox="gallery"]');
        }

      } catch (err) {
        console.error(err);
      }
    }

    loadGallery();
  })();
</script>